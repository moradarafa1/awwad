<?xml version="1.0" encoding="utf-8"?>
<!-- Home-screen widget (RemoteViews-safe views only). All three texts are
     pre-localized by the Dart side (HomeWidgetSync.push), so the layout is
     locale-neutral: centered, no start/end alignment to worry about in RTL. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/aw_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/awwad_widget_bg"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp">

    <TextView
        android:id="@+id/aw_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#ECEFF4"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/aw_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#2DD4BF"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/aw_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:background="@drawable/awwad_widget_btn"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:textColor="#0A0E14"
        android:textSize="12sp"
        android:textStyle="bold" />
</LinearLayout>
