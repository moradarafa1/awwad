# خطة توجيه المالك 2026-07-18 (المدموجة من بحث المحاور الأربعة)

> المصدر: workflow wf_93a19fab-46a (5 وكلاء بحث + دمج). هذا الملف هو المرجع التنفيذي.
> حدّث حالة كل جولة هنا عند إنجازها (DONE/PARTIAL/PENDING) وأضف سطر changelog في PROJECT_STATE.
>
> منجز مسبقاً قبل كتابة هذا الملف (جولات 4-6 في changelog يوم 2026-07-18): المنبهات الدقيقة
> للصلاة والأذان (أساس N2)، ودجت الشاشة الرئيسية أندرويد + توأمه iOS في المستودع، عنصر
> Round 4 N6 (سقف 30 تذكيراً على iOS)، رسالة «أندرويد فقط» الصادقة في شاشة الاستخدام على
> iOS، خطوات iOS الحقيقية في درع DNS، مستوى timeSensitive لإشعارات الصلاة على iOS،
> وإصلاحا المراجعة اللغويان (n%100 والفرنسية à).

## Round 1 - Notification reliability core (app build/deploy; the core promise must not silently break)

- [x] DONE 2026-07-18 r7: [notif N3, high/small] Add a proper monochrome status-bar icon: white-on-transparent ic_stat_awwad in res/drawable-*dpi, reference it in AndroidInitializationSettings (notifications_mobile.dart:55) and in UsageLimitWorker.kt setSmallIcon (:109), plus accent color on Android details. Every notification currently renders as a grey blob.
- [x] DONE 2026-07-19 r16 (window raised to the id-scheme maximum of 10d Android / 2d iOS, locked by prayer_window_test; background re-arm deliberately NOT built, see the code comment): [notif N1, high/medium] Prayer/adhan alarms die if the app is closed 2 days: raise the scheduling window from days=2 to 6 in prayer_engine.dart:205 (id scheme supports it collision-free), then add a daily background re-arm (native periodic WorkManager job reusing the UsageLimitWorker pattern, or a headless Dart isolate; the astronomical engine is pure Dart/offline). Also re-arm the monthly report (id 1005) and handle TIMEZONE_CHANGED/TIME_SET so absolute-time alarms self-correct after travel.
- [~] PARTIAL 2026-07-18 r7 (manifest comment matrix fixed + schedule-time inexact degrade already shipped; state-change receiver pending): [notif N2, high/medium] Exact prayer alarms are permanently DELETED on reboot when the Android 12+ exact-alarm grant is revoked (plugin removeNotificationFromCache path): add a native receiver for AlarmManager.ACTION_SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED that triggers a reschedule, schedule the prayer family redundantly inexact as a safety net, and fix the wrong manifest comment (exact alarm is default-DENIED on Android 14+ new installs, not 'auto on 13+').
- [x] DONE 2026-07-18 r8: [notif N5, medium/small] POST_NOTIFICATIONS dead end: after permanent denial, replace the snackbar with a dialog deep-linking to ACTION_APP_NOTIFICATION_SETTINGS (via the existing awwad/reliability channel); on each home_shell open reconcile the in-app toggle against areNotificationsEnabled()/iOS checkPermissions so the toggle never lies.
- [x] DONE 2026-07-18 r7: [notif N9, low/small] Timezone-lookup failure currently falls back to UTC, shifting every reminder by hours: fall back to a fixed-offset location from DateTime.now().timeZoneOffset instead (notifications_mobile.dart:45-53) + one-time debug flag.
- [x] DONE 2026-07-19 r13 (cancel guards + authoritative id/channel map added; dedupe purge left to the worker): [notif N10, low/small] Housekeeping in the same files: cancel the 15-min awwad_usage_guard worker when no app limits exist (MainActivity.kt:22-30) and enqueue only when they do; purge non-today 'notified_<pkg>_<day>' dedupe prefs each worker run; consolidate ONE authoritative notification-id map in notifications_mobile.dart (1003/1004/1005/1998/1999/2000+/3000+/4000-4300); exception-guard the cancel calls at :450-468 so a MissingPluginException cannot abort applyNotificationSchedule.

## Round 2 - Per-habit content correctness (owner's 0a) + quick retention wins (app build/deploy)

- [x] DONE 2026-07-19 r10: [ux CU2, high/small] break_porn (the flagship recovery habit) falls back to generic urge/resistance sliders because the secret_habit alias exists only in habitChecklistLabels: add a duplicate 'break_porn' entry to kHabitMetricsOverrides in the generated map (safer than touching 4 callsites); verify SOS and stats render the aliased content consistently.
- [x] DONE 2026-07-19 r10: [ux CU1, high/medium] Fill the content holes for surah_kahf, listening_wird, hadith_wird (absent from kHabitChecklists, kExtraCompeting, kExtraEnvironment AND kHabitMetricsOverrides): tailored metrics + 5/4 checklist items each via scratchpad/assemble_daily_content.mjs. Critically, surah_kahf asks a WEEKLY question while the streak engine counts DAILY: add a weekly-unit streak mode in app_state.dart or minimally auto-excuse non-Friday days so its streak tells the truth.
- [x] DONE 2026-07-19 r10: [ux CU7, medium/small] Restore the stripped French accents across ~8 break habits in habit_content.dart (logged 2026-07-17, still open): scripted programmatic sweep + diff review, then analyze/tests; close the PROJECT_STATE logged minor.
- [x] DONE 2026-07-19 r10: [ux CU13, low/small] defaultReminderHours for the habits that obviously need them: sleep_early + late_nights [21], qiyam [21], voluntary_fasting [20], exercise/read_books one evening slot, istighfar [8,13,20]. Catalog constructor args only; no seed/DB sync needed (reminders are app-only per section 8).
- [x] DONE 2026-07-19 r10: [ux CU4, high/small] Personal-record celebration: '🏆 أفضل سلسلة: N' chip next to the rank line on Today; in saveEntry detect currentStreak > previous longestStreak and fire the existing confetti (badge_celebration.dart) with a 'رقم قياسي جديد' variant + tray notification via showBadgeNotification.
- [x] DONE 2026-07-19 r10: [ux CU5, medium/small] Surface money-saved on Today (Quitzilla's flagship): compact savings tile in the Today stats row whenever habit.costPerDay > 0 ('وفّرت X' = costPerDay * cleanDays, taps into the stats SavingsCard); one gentle one-time cost prompt for quit_smoking/quit_vaping/junk_food/impulse_buying/caffeine_excess users who skipped the field.
- [x] DONE 2026-07-19 r10: [ux CU6, medium/small] Daily rotating quote card: kDailyMotivation in motivation.dart from already-verified in-repo content (kRecoveryTimeline, habit_stages tips, Sahih Muslim dhikr, catalog descriptions) + ~20 new MSA trilingual lines, deterministic by dayKey hash + track, gated on settings.showReligiousContent, offline, no em-dash.
- [x] DONE 2026-07-19 r10: [ux CU12, low/small] Streak-freeze visibility: passive chips near the streak tile showing remaining excuse-day tokens ('🧊 إعفاءات متبقية: ٢') from the existing weeklySkipUsage/monthlySkipUsage getters; no logic change.

## Round 3 - Site technical SEO round (one Astro build + Pages deploy)

- [x] DONE 2026-07-19 r9: [seo SA4, high/small] Fix the canonical/hreflang/og:url trailing-slash mismatch (all ~108 non-home pages declare a canonical that 301-redirects, conflicting with the sitemap): trailingSlash 'always' in astro.config.mjs + append '/' in Base.astro localizedSlug/localizedPath outputs and [...path].astro articleLd.mainEntityOfPage; spot-check dist for /break-habit/, /en/break-habit/, one blog post per locale.
- [x] DONE 2026-07-19 r9: [seo SA5, high/small] Put target keywords in the home title tag and H1 (currently brand + slogan only): AR title like «عوّاد: تطبيق مجاني لترك العادات السيئة وبناء عادات جديدة» (+EN/FR equivalents), render the keyword-bearing page.h1 and demote the sub line to a <p>; keep the slogan in the visible hero pill.
- [x] DONE 2026-07-19 r9: [seo SA7, medium/medium] Internal links (90 article pages currently have zero contextual links): 'مقالات ذات صلة' block on every post (3 same-category posts + matching hub link), a 'من المدوّنة' section on break-habit/build-habit hubs (5-6 relevant articles each), home page links to blog index + 2-3 flagship articles.
- [x] DONE 2026-07-19 r9: [seo SA8, medium/medium] JSON-LD upgrade: Organization.logo; MobileApplication url/description/image/inLanguage/applicationSubCategory; BreadcrumbList on posts + hubs; Article image + dateModified + publisher.logo; og:type='article' + article:published_time in Base.astro for post pages.
- [x] DONE 2026-07-19 r9: [seo SA9, medium/medium] Core Web Vitals: self-host Cairo subset to the weights actually used (audit first, likely 400/700/900) as woff2 in public/fonts/ with font-display:swap + preload (also preload SalmaArabic-Bold.otf); drop the render-blocking Google Fonts request entirely (site becomes 100% first-party, matching the cookie-free claim); replace body background-attachment:fixed 3-gradient jank with a fixed-position pseudo-element or a media-query fallback.
- [x] DONE 2026-07-19 r9: [seo SA10, low/small] Sitemap: @astrojs/sitemap i18n option for per-URL xhtml:link hreflang alternates (ar root / en / fr) + serialize() lastmod from post.date.

## Round 4 - Notification architecture + permission UX polish, incl. iOS parity code (app build/deploy)

- [x] DONE 2026-07-19 r13: [notif N4, medium/medium] Coherent, localized notification channels: dedicated channels for prayer-mains/pre-alerts/adhkar (separate from habit awwad_daily so muting habit nudges cannot kill prayer alerts), pomodoro, and monthly report (stop flip-flopping the awwad_badges description); localize all channel names/descriptions to the saved app locale (safe on re-creation with same id); deliberate showBadge/vibration per channel; keep ids stable per the awwad_adhan_v1 sound-immutability pattern (also localize 'Usage limits' in UsageLimitWorker.kt:93).
- [x] DONE 2026-07-18 r8 + r16 (iOS habit slots capped at 30 and the prayer window held at 2 days on iOS, so prayer + habits + singletons stay under 64; Android keeps 60 slots / 10 days): [notif N6, medium/medium] iOS 64-pending-notification cap: per-platform budget in applyNotificationSchedule/applyPrayerSchedule - on iOS always schedule the prayer family FIRST and cap habit reminders (~30) or aggregate them, so prayer/monthly one-offs are never silently dropped.
- [ ] [notif N8, medium/medium] Notification tap routing + iOS gaps: wire onDidReceiveNotificationResponse/background handler + payloads on BOTH platforms (prayer notif -> prayer screen, habit reminder -> that habit's log; a high-feel win for mandate (a)); bundle a <30s adhan .caf and set DarwinNotificationDetails(sound:); add DarwinNotificationCategory quick actions (e.g. 'سجلت' quick-log mirroring the Android widget); add the v18 UNUserNotificationCenter delegate wiring in AppDelegate.swift (on-device verification is Mac-gated, see owner list).
- [ ] [policy SP9, medium/medium] Move the OS notification permission prompt out of first-launch AuthChoiceScreen into context (when the user sets reminder times in onboarding, enables prayer notifications, or toggles Settings), with a one-line pre-prompt; raises grant rates and removes a known Apple soft-rejection flag. Apply on both platforms for one code path.
- [ ] [notif N7 partial, medium] Adhan vs DND: add the cheap safe piece now - new awwad_adhan_v2 channel created natively with setBypassDnd (plugin does not expose it; tiny native channel-creation hook) + an honest note in prayer settings that adhan follows alarm volume and DND behavior. The full-screen-intent alarm-style wake path is OWNER-GATED (Play FSI declaration + opt-in policy decision, see owner list).

## Round 5 - Flagship competitive features on existing habits (app build/deploy)

- [x] DONE 2026-07-19 r14: [ux CU3, high/medium] Tasbih tap-counter (عداد الذكر): new core/widgets/tasbih_counter.dart (big tap target, haptic, optional 33/100 target, daily reset) in daily_log_screen for istighfar/salawat/adhkar/gratitude/dua, mapping count onto the primary metric (100+ taps = 10) so storage/sync/stats need zero schema change; embed it in the SOS dhikr step. Direct differentiator no Western habit app has; fully offline.
- [x] DONE 2026-07-19 r15: [ux CU10, medium/medium] SOS outcome loop: on wave completion a 'مرّت الموجة، الحمد لله' prompt with 'صمدت' (sos_survived analytic + gentle confetti) and 'تعثرت' (deep-link to today's log with the slip chip preselected so the trigger gets journaled); surface the واعي resource + curated kHabitVideos as one-tap escape shortcuts inside SOS for secret_habit/break_porn; add the tasbih counter as a quick action.
- [x] DONE 2026-07-19 r14: [ux CU9, medium/medium] Habit strength score (Loop-style): pure habitStrength EWMA getter in app_state.dart (half-life ~14 days, skip days neutral via existing isSkip), unit tests beside the rolling-window skip-quota tests, small ring/percent chip beside the rank line on Today and on Stats. Streak for motivation, strength for resilience - answers the 'one bad day erased weeks' churn complaint.
- [x] DONE 2026-07-19 r15: [ux CU8, medium/medium] Weekly insight card: pure computeWeeklyInsight() (mirroring monthly_report.dart, unit-testable) over the last 7-14 activeEntries - dominant slip trigger, cleanest weekday, urge trend vs prior week - rendered on Stats (or Today every 7th log) with one actionable MSA sentence per dominant trigger.

## Round 6 - Verified scholar-video curation for 15 break habits (research workflow + app build)

- [x] DONE 2026-07-19 r18 (13 habits gained a mechanically-verified video; impulse_buying deliberately left empty at 905s vs the 900s rule; dead search fallback deleted): [ux CU11, medium/medium] Run the established research + adversarial-verify workflow to curate 1-2 verified <30-min videos per habit from the approved scholars for the 15 break habits that lost their card when the curated-only rule landed (quit_smoking, quit_vaping, nail_biting, hair_pulling, skin_picking, phone_addiction, excessive_gaming, procrastination, junk_food, oversleeping, bad_language, impulse_buying, caffeine_excess, late_nights, binge_watching, anger); regenerate kHabitVideos via assemble_daily_content.mjs and delete the now-dead kHabitVideoQuery + habitVideoSearchUrl code (habit_content.dart:29-34, 312-330). The research workflow can be started in parallel during rounds 4-5; this round is the merge + build.

## Round 7 - Store-compliance code changes (app build/deploy; hard submission blockers)

- [x] DONE 2026-07-19 r11: [policy SP1, high/medium] In-app account deletion (required by Play account-deletion policy AND Apple 5.1.1(v); the current web-only approach is a standard rejection on both stores): native flow in Settings account card/ProfileScreen next to sign-out - confirm, call the existing account-export-delete edge function with the signed-in JWT, wipe local data, sign out; the unused l10n deleteAccount strings already exist; keep the web page as the required Play web-link resource. PREREQUISITE: inform the owner his 2026-07-12 web-only decision conflicts with written policy (see owner-gated list) - build it ready in this round either way.
- [x] DONE 2026-07-19 r11: [policy SP7, medium/small] Minimum location scope: remove ACCESS_FINE_LOCATION from AndroidManifest (lines 23-24; nearest-city matching over 306 cities needs only km-level accuracy) and request geolocator with low/reduced accuracy in prayer_settings_screen.dart; verify post-build that aapt no longer lists FINE. Future-proofs against the 2026-10-28 precise-location regime.
- [x] DONE 2026-07-19 r11: [policy SP3 in-app part, high/small] Add the missing sharing sentence to the PACKAGE_USAGE_STATS prominent disclosure in usage_screen.dart (ar/en/fr): «تبقى هذه البيانات على جهازك فقط ولا تُرسل إلى خوادمنا ولا تُشارك مع أي جهة» - true today and must stay true.

## Round 8 - Store metadata + submission docs (docs/listing files only; no app build)

- [x] DONE 2026-07-19 r12: [seo SA1 = policy SP4, high/small, MERGED DUPLICATE] Replace every dead Netlify URL in docs/store/STORE_LISTINGS.md (10 refs) and SUBMISSION_GUIDE.md (3 refs) with the live GitHub Pages URLs (site https://moradarafa1.github.io, app /app/, /privacy, /delete-account) in all three languages; verify the delete-account page loads, prominently features the deletion path, and names «عوّاد / Awwad» exactly as listed. A dead privacy-policy URL is an automatic Play rejection.
- [x] DONE 2026-07-19 r12: [seo SA3 + policy SP2 claim-fix, high/medium, MERGED] Rewrite the store listings for the shipped product: 40 habits (not 36); add the strongest missing ASO features (مواقيت الصلاة والأذان، إذاعة القرآن والحديث والورد الصوتي، مراقبة استخدام الهاتف وحدوده وعدد مرات الفتح، ودجت الشاشة الرئيسية، زر الهدنة/SOS، التقرير الشهري); AR short description working in «تتبع العادات»; iOS AR keyword field stripped of «ال» + اذان/مواقيت/اذكار/هاتف; EN 'adhan'/'prayer times'; explicitly promote the usage-monitoring toolkit (required so PACKAGE_USAGE_STATS maps to promoted core functionality per SP3); AND soften the untruthful «بياناتك تبقى على جهازك» privacy claim to the accurate offline-first + optional-account + minimal-anonymous-analytics version. MSA, no em-dash, re-verify character limits programmatically.
- [x] DONE 2026-07-19 r12: [policy SP2 docs part, high/medium] Write the exact Play Data-safety answers + Apple privacy nutrition labels into SUBMISSION_GUIDE.md matching real flows: Personal info (name, email, gender, optional DOB/phone), Health info (habit logs incl. smoking/porn/sleep), App interactions (analytics events); encrypted in transit, user-deletable, not shared, no ads; deletion URL; precise guest-mode statement (only anonymous analytics leave the device).
- [x] DONE 2026-07-19 r12: [policy SP3 docs part, high/small] Ready-made PACKAGE_USAGE_STATS Console justification paragraph in SUBMISSION_GUIDE.md (digital-wellbeing self-monitoring for the phone-addiction habit, user-initiated, foreground + 15-min limit worker, no data leaves the device); note the launcher <queries> block is intent-scoped so no QUERY_ALL_PACKAGES declaration is needed.
- [x] DONE 2026-07-19 r12: [policy SP5, medium/small] Document the content-rating/target-audience answers: 13+/16+ target audience (NEVER include children - avoids the entire Families policy set), IARC answered honestly re sexual-health self-help topics (expect Teen/Mature in some regions), Apple 17+ as the safe answer; verify listing text stays free of explicit wording.
- [x] DONE 2026-07-19 r12: [policy SP6, medium/small] Document the Play Health-apps declaration answers (wellness/habit-tracking, NO medical device, NO Health Connect), pick the category deliberately (Lifestyle defensible), and sweep listing + in-app copy once for علاج/شفاء treatment claims (current ترك/الإقلاع wording is fine).
- [x] DONE 2026-07-19 r12: [policy SP8, medium/small] Exact-alarms documentation: reviewer justification in SUBMISSION_GUIDE.md (user-scheduled time-critical religious reminders, graceful degrade, never engagement pings); record the DO-NOT rule that USE_EXACT_ALARM must never be added (Play blocks non-alarm-clock apps); confirm aapt shows SCHEDULE_EXACT_ALARM in the next release checklist.
- [x] DONE 2026-07-19 r12: [policy SP10, low/small] Record the verified-compliant items in SUBMISSION_GUIDE.md so nothing is re-litigated: targetSdk 36 (Flutter 3.44.4 default) vs API 35+ requirement, Sign in with Apple NOT required (own email+password only; never add social login without it), UIBackgroundModes audio justified by real playback, guest mode satisfies Apple 5.1.1(ii), religious content pipeline (islamweb-verified + no-fatwa disclaimer) and stream-source rights documented, no UGC surface.

## Round 9 - Store screenshots + visual assets + PWA polish (assets + one web rebuild/redeploy)

- [~] PARTIAL 2026-07-19 r16 (real 1125x2436 captures now possible via ops/shotgen/capture.mjs; ar set captured, en/fr + feature graphic pending): [seo SA2, high/large] Produce the store screenshots (assets/screenshots/ is EMPTY; Play requires minimum 2 to publish and screenshots are the biggest conversion lever): 6-8 Arabic RTL phone shots (1080x1920+) with benefit captions - Today + habit switcher, prayer times + adhan, usage monitoring with open counts + limits, stats/badges shields, SOS/هدنة, Quran/hadith radio, Pomodoro, home widget - captured from the release APK on an emulator or the web app at mobile viewport; EN/FR caption variants; refresh the 1024x500 feature graphic with real UI + the official sprout logo (never redraw it, section 11 rule); scripted caption-overlay composition kept in ops/; add the screenshot spec section to STORE_LISTINGS.md.
- [x] DONE 2026-07-19 r12: [seo SA11, low/small] PWA manifest polish riding the same web rebuild: theme_color #4F8EF7 -> brand #0D1117, add id '/app/', categories, 1-2 shortcuts (quick log, SOS/هدنة), screenshots reused from SA2 for the richer install sheet; keep the intentional noindex; note the maximum-scale=1.0/user-scalable=no a11y flag while in the file. app/web/ is the build template - takes effect on the next flutter build web + Pages /app/ redeploy.

## Round 10 - Blog expansion: 8-9 high-intent trilingual articles (site deploys; research workflows can start in parallel from Round 3)

- [x] DONE 2026-07-19 r17: [seo SA6, high/large] Write the 8-9 missing high-intent trilingual articles via the established research + adversarial-verify workflow (MSA, no em-dash, Article + FAQPage JSON-LD, dated posts.js entries), in priority order: (1) ترك العادة السرية نهائياً (the single biggest Arabic habit-change query cluster; write with dignity per docs/content-values-guideline.md), (2) علاج إدمان الإباحية وغض البصر (ties to the DNS shield), (3) كيف أتحكم في غضبي, (4) إدمان مواقع التواصل الاجتماعي, (5) أفضل تطبيقات تتبع العادات للمسلمين (honest commercial listicle), (6) المحافظة على الصلاة في وقتها (targets تطبيق مواقيت الصلاة/أذان), (7) الصلاة على النبي ورداً يومياً, (8) بر الوالدين عادات عملية, (9) خطة عادات رمضان (publish well before Ramadan). Wire each into the Round 3 internal-linking blocks as it lands.

## OWNER-GATED (لا تُنفذ دون قرار المالك)

- SP1 decision reversal ACK: the owner's 2026-07-12 decision (web-page-only account deletion) directly violates Google Play's account-deletion policy and Apple 5.1.1(v) and is a standard rejection on both stores. Tell him plainly and get his acknowledgment; the in-app deletion flow is built ready in Round 7 regardless, since submission is impossible without it.
- Store submission itself (PROJECT_STATE section 12 P6): Play Console account ($25 one-time, the owner's money) + console access to create the listing, upload the AAB, and SUBMIT the Data-safety form, Health-apps declaration, IARC content-rating questionnaire, target-audience selection, and the PACKAGE_USAGE_STATS / exact-alarm questionnaires. Round 8 prepares every answer verbatim in SUBMISSION_GUIDE.md; only the owner can enter them in the Console.
- Apple/iOS submission chain: Apple Developer account ($99/yr) + a Mac with Xcode - store submission, the IOS_PARITY_SETUP.md steps (widget target, app group, adhan30.caf bundling), and on-device verification of the Round 4 iOS notification wiring (delegate, tap routing, .caf sound) all wait on this.
- N7 full-screen-intent adhan (true alarm-style wake through DND/bedtime): requires a Play FSI declaration, Android 14+ special-access request UI, and an explicit opt-in product decision - defensible for a prayer app but the owner must approve the policy exposure before it is built. The safe partial (DND-bypass channel + honest settings note) ships un-gated in Round 4.
- Custom domain purchase (the project's ONLY allowed paid item, owner's call): ideally decided BEFORE store submission so the privacy-policy / delete-account URLs baked into store metadata (Round 8) never churn; if bought, also update astro.config.mjs site, robots.txt, WEB_APP_URL and redeploy.
- Pre-existing owner-gated items from PROJECT_STATE section 12 that this plan deliberately does NOT touch: 0c phase C hard app-blocking (AccessibilityService, Play-rejection risk - owner must choose), the new «غض البصر» habit (needs catalog + seed + live DB sync approval), and FCM push / Brevo email sequences (P4).
- Deploy-permission standing rule: never deploy to Netlify without asking each time (owner is free-tier cost-sensitive); live hosting is GitHub Pages (moradarafa1.github.io) and each round's Pages push follows the established deploy runbook.
